@model Publications.Web.Areas.Identity.Models.UsersListModel

@{
    ViewBag.Title = "title";
    Layout = "_Layout";
}

<h2>@ViewBag.Title</h2>

<ul>
    @foreach (var user in Model)
    {
        <li>
            @user.UserName
        </li>
    }
</ul>
